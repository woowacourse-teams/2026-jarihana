# Account pages verification

- Scenario: OAuth callback re-checks auth and ignores a misleading `signupRequired` query.
  Invocation: `npm test -- --runTestsByPath tests/pages/account/oauth-and-signup.test.jsx tests/pages/account/my-page.test.jsx tests/pages/account/my-lists.test.jsx`
  Observable: 3 suites, 10 tests passed. Raw output: `jest.log`.
- Integration follow-up: callback and signup consume the validated return target only at successful navigation; signup uses `useSignupMember`; anonymous signup recovers through login; group role follows `?role=LEADER`; dashboard counts show all loaded items with `+` when another cursor exists; internal recovery links use React Router.
  Invocation: `npm test -- --runTestsByPath tests/pages/account/oauth-and-signup.test.jsx tests/pages/account/my-page.test.jsx tests/pages/account/my-lists.test.jsx`
  Observable: 3 suites, 12 tests passed. Raw output: `jest-integration.log`.
- Scenario: signup validates Hangul crew name, positive generation and course; successful signup reloads auth; conflict is announced safely.
  Invocation and observable: same focused Jest run, green.
- Scenario: My dashboard renders the backend-shaped member, group and registration data without a fake profile edit action.
  Invocation and observable: same focused Jest run, green.
- Scenario: My groups and registrations expose cursor paging, filter state, recovery and confirmed withdrawal.
  Invocation and observable: same focused Jest run, green.
- Scenario: owned JavaScript/JSX and tests meet repository lint rules.
  Invocation: `npx eslint src/pages/account tests/pages/account`
  Observable: exit 0 with no warnings or errors.
- Source integrity: `git diff --check -- frontend/src/pages/account frontend/tests/pages/account`
  Observable: exit 0.

- Integrated production compilation: `npm run build`
  Observable: webpack exit 0. Raw output: `build-integration.log`. Webpack reports only its existing asset/entrypoint size warnings.
- CSS token audit: every `var(--color-*)` used by `account.css` exists in `shared/styles/tokens.css`; no undefined fallbacks and no raw hex/rgb values were found. Layout-specific pixel geometry follows the documented spacing, radius, container and breakpoint decisions; no shared token addition is required.
- Contrast follow-up: all account text formerly using decorative `--color-brand-strong` or low-contrast `--color-muted` now uses `--color-text-brand` / `--color-text-muted`. Focused account tests remain 12/12 green and account ESLint exits 0; `rg` confirms no stale text-color declarations.
- React Doctor performance follow-up: `formatKoreanDate` now reuses one module-scoped Korean `Intl.DateTimeFormat` while preserving the existing local `Date` conversion. A RED test observed two formatter constructions before the fix; the final account run passes 4 suites / 13 tests and ESLint exits 0.
- Withdrawal focus follow-up: after a successful confirmed withdrawal removes its opener, the page waits until the dialog's focus-return frame completes and programmatically focuses the persistent `신청 목록` heading (`tabIndex=-1`). The targeted test rerenders with the registration removed and observes the heading owning focus; all 4 account suites / 13 tests and ESLint pass. Rejected mutations do not invoke the success callback, so the shared dialog remains open with its existing retry/focus behavior.
- Signup continuation race follow-up: `SignupPage` now only awaits the member mutation and `reload()`; it neither consumes the saved return target nor calls navigation. `SignupGuard` is therefore the sole continuation owner after auth becomes authenticated. The RED test observed the former page-side consume; the final focused account run passes 4 suites / 13 tests and ESLint exits 0.
- MyPage Figma fidelity follow-up: the summary-only duplicate navigation was removed so the Figma-style activity counts own the right-panel navigation; the page now uses the full 85rem shell, 270px profile/companion rail, denser right activity panel, 140px cards, tighter section rhythm, and explicit tablet/mobile adaptations. Repeated spacing, typography, radii, border widths, and touch targets use shared tokens. CSS contract audit produced no undefined variables, raw colors, raw border/radius values, or raw font-size declarations. Focused account verification remains 4 suites / 13 tests with ESLint and `git diff --check` clean.

## VIS-2 / CJK fidelity verification (2026-08-21)

- Scenario: the authenticated My page renders a Figma-shaped, bordered primary activity surface with compact two-column rows and an internal pagination footer, while every displayed group/registration and every enabled paging action comes from the three real infinite-query results. Invocation: `npm test -- --runTestsByPath tests/pages/account/my-page.test.jsx`. Binary observable: exit 0; 1 suite / 1 test passed, including the `내 활동 보드` region and an enabled `다음 활동 불러오기` control that invokes the supplied `fetchNextPage` exactly once. Captured implementation/test artifacts: `frontend/src/pages/account/MyActivityBoard.jsx`, `frontend/src/pages/account/MyPage.jsx`, `frontend/tests/pages/account/my-page.test.jsx`.
- Scenario: a 360px account composition keeps `가입한 모임`, `신청한 모임`, and `운영하는 모임` intact rather than splitting a Korean word, and reflows the third count to a full-width row; OAuth callback uses this same MyPage after its authenticated redirect. Invocation: source contract inspection plus the focused account Jest command below. Binary observable: the rendered labels carry `dashboard-counts__label`; `account.css` defines `white-space: nowrap`, `word-break: keep-all`, a two-column mobile grid, and a full-row final item. Captured artifact: `frontend/src/pages/account/account.css`.
- Scenario: all owned account behavior remains green after the fidelity change. Invocation: `npm test -- --runTestsByPath tests/pages/account/account-utils.test.js tests/pages/account/oauth-and-signup.test.jsx tests/pages/account/my-page.test.jsx tests/pages/account/my-lists.test.jsx`. Binary observable: exit 0; 4 suites / 13 tests passed. Captured artifact: this verification record and the four files under `frontend/tests/pages/account/`.
- Scenario: owned source satisfies static, formatting, and production compilation gates. Invocations: `npx eslint src/pages/account tests/pages/account`; `npx prettier --check src/pages/account tests/pages/account`; `npm run build`. Binary observables: ESLint exit 0 with no output; Prettier reports all matched files formatted; webpack exits 0 and emits `assets/main.0bd81e5d.js`, with only the known asset/entrypoint-size warnings.
- Scenario: account styling uses defined semantic tokens without raw colors or raw border/radius/type declarations. Invocation: variable-set `comm`, raw-value `rg`, and `git diff --check` audits against `src/pages/account/account.css` and `src/shared/styles/tokens.css`. Binary observable: all three commands exit 0 with no output. Captured artifact: `frontend/src/pages/account/account.css`.

## 360px Korean wrapping verification (2026-08-21)

- RED scenario: the group-card declarations did not preserve Korean word boundaries, and the signup heading had neither a balancing rule nor a dedicated DOM hook. Invocation: `npm test -- --runTestsByPath tests/pages/account/account-css.test.js tests/pages/account/my-lists.test.jsx tests/pages/account/oauth-and-signup.test.jsx`. Binary observable before implementation: 2 suites failed / 1 passed; three assertions failed for absent `word-break: keep-all`, `overflow-wrap: anywhere`, `text-wrap: balance`, and `account-heading__title`.
- GREEN scenario `/my/groups` at 360px: a backend-shaped description containing `테스트 세션` renders in an `.account-card`; account cards and their headings/descriptions have `min-width: 0`, preserve Korean words with `word-break: keep-all`, and retain `overflow-wrap: anywhere` only as the narrow-width fallback for an otherwise unbreakable token. Captured source/test artifacts: `frontend/src/pages/account/account.css`, `frontend/src/pages/account/AccountCards.jsx`, `frontend/tests/pages/account/account-css.test.js`, `frontend/tests/pages/account/my-lists.test.jsx`.
- GREEN scenario `/signup` at 360px: the exact heading `자리하나에서 사용할 정보를 알려 주세요` renders with `account-heading__title`; its style uses `text-wrap: balance` and `word-break: keep-all` without reducing `--text-h1`. The account prose audit also applies the safe keep-all/fallback pair to account `h1`, `h2`, `h3`, and `p` content, while existing no-wrap controls remain unchanged. Captured source/test artifacts: `frontend/src/pages/account/AccountLayout.jsx`, `frontend/src/pages/account/account.css`, `frontend/tests/pages/account/oauth-and-signup.test.jsx`.
- Final regression invocation: `npm test -- --runTestsByPath tests/pages/account/account-css.test.js tests/pages/account/account-utils.test.js tests/pages/account/oauth-and-signup.test.jsx tests/pages/account/my-page.test.jsx tests/pages/account/my-lists.test.jsx`. Binary observable: exit 0; 5 suites / 15 tests passed.
- Static/format/build invocations: `npx eslint src/pages/account tests/pages/account`; `npx prettier --check src/pages/account tests/pages/account`; `npm run build`. Binary observables: ESLint exit 0 with no output; Prettier reports all files matched; webpack exit 0 emitted `assets/main.8b02ad7e.js`, with only the known asset/entrypoint-size warnings. Token/raw-value and `git diff --check` audits also exit 0 with no violations.

The root executor owns final browser and visual QA across the integrated router.
