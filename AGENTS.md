# 자리하나 공통 AI 작업 지침

## 저장소 구조

이 저장소는 다음 애플리케이션과 공통 문서로 구성된 모노레포다.

- `backend/`: Spring Boot 백엔드 애플리케이션
- `frontend/`: React 프론트엔드 애플리케이션
- `infra/`: 배포와 운영 인프라 설정
- `docs/`: 저장소 전체에 적용되는 공통 문서

## 작업 시작 순서

모든 작업은 다음 순서로 시작한다.

1. 현재 브랜치와 working tree 상태를 확인한다.
2. 공통 브랜치·PR·커밋 정책은 [`docs/workflow.md`](docs/workflow.md)를 기준으로 확인한다.
3. 작업 범위를 분류하고 해당 디렉터리의 세부 지침을 직접 읽는다.
   - 백엔드 작업: `backend/AGENTS.md`
   - 프론트엔드 작업: `frontend/AGENTS.md`, `frontend/docs/IMPLEMENTATION_MAP.md`
   - 인프라 작업: `infra/AGENTS.md`가 있으면 해당 파일
4. 세부 지침이 지정한 설계 문서, 실행 명령과 검증 절차를 추가로 확인한다.
5. 확인한 범위 안에서만 가장 작은 변경을 수행한다.

## 공통 작업 규칙

- 현재 사용자의 요청을 가장 우선한다.
- 작업 전에 확인한 기존 변경사항을 되돌리거나 덮어쓰지 않는다.
- working tree가 dirty하면 기존 변경과 겹치는 파일을 먼저 식별하고 보존한다.
- 명시적인 요청 없이 브랜치를 전환하거나 보호 브랜치에 직접 작업하지 않는다.
- 요청 범위를 벗어난 정리, 의존성 변경, 파일 이동과 리팩터링을 추가하지 않는다.
- 공통 규칙은 이 파일에, 백엔드·프론트엔드·infra 전용 규칙은 각 하위 `AGENTS.md`에 둔다.
- 문서의 정책과 실행 가능한 코드·설정이 다르면 차이를 숨기지 말고 현재 요청 범위 안에서 보고한다.

## 구현과 검증

- 구현 전 관련 코드, 설정, 테스트와 실행 가능한 저장소 명령을 확인한다.
- 동작이나 계약을 변경한 경우 해당 디렉터리의 규칙에 따라 관련 테스트와 정적 검증을 실행한다.
- 백엔드 검증은 `backend/AGENTS.md`와 백엔드 문서가 지정한 Gradle 명령을 따른다.
- 프론트엔드 검증은 `frontend/AGENTS.md`와 프론트엔드 문서가 지정한 lint, test, build 및 필요한 브라우저 확인을 따른다.
- 검증할 수 없는 항목은 성공했다고 가정하지 말고, 이유와 남은 검증 공백을 보고한다.
- 최종 보고에는 변경 파일, 확인한 검증 결과와 남겨 둔 이슈를 포함한다.

## 지침 우선순위

충돌이 생기면 다음 순서로 판단한다.

1. 현재 사용자의 명시적 요청
2. 이 파일의 공통 규칙
3. 작업 대상 디렉터리의 `AGENTS.md`
4. 해당 디렉터리의 설계·컨벤션 문서
5. 저장소의 코드, 테스트와 설정에서 확인한 실행 가능한 사실
