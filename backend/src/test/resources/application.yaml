server:
  servlet:
    context-path: /api

spring:
  datasource:
    # Testcontainers가 운영과 같은 postgres:17 컨테이너를 띄우고, 같은 URL을 쓰는 Spring 컨텍스트가 함께 쓴다.
    # TC_DAEMON=true는 커넥션이 모두 닫혀도 JVM이 끝날 때까지 컨테이너를 유지한다.
    url: jdbc:tc:postgresql:17:///jarihana_test?TC_DAEMON=true
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  session:
    jdbc:
      initialize-schema: always

jarihana:
  image:
    public-base-url: https://cdn.example.test/images
  auth:
    frontend-origin: http://localhost:5173
    oauth-state-cookie-name: oauthState
    refresh-token-validity: P14D
    cookie:
      secure: false
      access-token-name: accessToken
      access-token-path: /
      refresh-token-name: refreshToken
      refresh-token-path: /api/auth
    jwt:
      secret: test-access-token-secret-key-for-hmac-sha256-0001
      validity: PT1H
  oauth:
    github:
      client-id: test-client-id
      client-secret: test-client-secret
      redirect-uri: http://localhost:8080/api/oauth/github/callback
      token-uri: https://github.com/login/oauth/access_token
      user-uri: https://api.github.com/user
